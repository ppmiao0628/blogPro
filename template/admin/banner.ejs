<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>管理员登录</title>
</head>
<body>
    <div>
        <% include components/navi.ppm.ejs %>
        <br>
        <div>
            <form action="?" method="post">
                标题：<input type="text" name="title"><br>
                描述：<textarea name="description"></textarea><br>
                链接：<input type="text" name="href"><br>
                <input type="submit" value="添加">
            </form>
            <table border="1" width="100%">
                <%= bannerObj %>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>标题</th>
                        <th>内容</th>
                        <th>地址</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <% for (var i=0;i<bannerObj.length;i++){%>
                <tbody>
                    <tr>
                        <td><%=bannerObj[i].ID%></td>
                        <td><%=bannerObj[i].title%></td>
                        <td><%=bannerObj[i].description%></td>
                        <td><%=bannerObj[i].href%></td>
                        <td>
                            <a href="?act=mod&id=<%=bannerObj[i].ID%>">修改</a>
                            <a href="?act=del&id=<%=bannerObj[i].ID%>" onclick="return confirm('你真的要删除吗')">删除</a>
                        </td>
                    </tr>
                </tbody>
                <%}%>
            </table>
            <br>
            <div>
                修改
            </div>
        </div>
    </div>
</body>
</html>